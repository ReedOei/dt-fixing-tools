To generate the database used for the ICST 2020 submission and the 536-all-mapping.csv file, do the following.
(1) Generate the database. Run: bash build-database.sh ../../complete_output/idflakies subject-info.db docker/data/full_list-ish.csv docker/data/full_subject_loc.csv
(2) Once the database is generated, import the csv test-fs-mod-map-fixed.csv by running: .import test-fs-mod-map-fixed.csv fs_test_mod_map
    This CSV file is generate from the following commands.    
    cat all-mapping.csv | cut -f9,11 -d, | sort -u | grep -v ",$" | while read l; do sha=$(echo $l | cut -f2 -d, | cut -b-7); name=$(echo $l | cut -f1 -d,); echo === $name $sha >&2; if [[ $(grep "${sha}" /tmp/oo | xargs grep "$name$" /dev/null | rev | cut -f2 -d/ | rev | sort -u | wc -l) == 1 ]]; then mod=$(grep "${sha}" /tmp/oo | xargs grep "$name$" /dev/null | rev | cut -f2 -d/ | rev | sort -u); echo "$name,$sha,$mod"; fi; done > test-fs-mod-map-fixed.csv
(3) To generate the 536-all-mapping.csv, run the following inside the database: select * from fs_idflakies_vers_results_all_filter;